# __externalClassNames

Use `__externalClassNames` to attach additional class names.

## Feature descriptions

- Value is a space-delimited string.
- These classes are appended to the generated internal class name.
- This key does not become a CSS property.

## Example

```json
{
  "__externalClassNames": "main-wrapper hero-section"
}
```
